/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "scontent-fra3-2.cdninstagram.com"],
  },
};

module.exports = nextConfig;
